<?php

$a = (true and false);	// And	TRUE if both $a and $b are TRUE.
$a = (true or  false);  // Or	TRUE if either $a or $b is TRUE.
$a = (true xor false);	// Xor	TRUE if either $a or $b is TRUE, but not both.
$a = (!false);	        // Not	TRUE if $a is not TRUE.
$a = (true && true);	  // And	TRUE if both $a and $b are TRUE.
$a = (true || true);  // Or	TRUE if either $a or $b is TRUE.

?>
  